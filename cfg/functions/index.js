const compareStrings = require('./compare-strings');
const filterTags = require('./filter-tags');
const incline = require('./incline');
const parseDuration = require('./parse-duration');

module.exports = { compareStrings, filterTags, incline, parseDuration };
